import { ButtoncolourDirective } from './buttoncolour.directive';

describe('ButtoncolourDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtoncolourDirective();
    expect(directive).toBeTruthy();
  });
});
